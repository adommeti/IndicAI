# Blockers

Items a build prompt could not complete in-session. One entry per item; remove the entry in the
PR that resolves it.

| date | prompt | what | why | what unblocks it | owner |
|---|---|---|---|---|---|
| 2026-09-18 | uc2/P2-eval | none of the four B6 numbers this prompt exists to produce (`fidelity_mean`, `terminology_adherence`, `terminology_adherence_pre_edit`, `timing_fit_rate`) could be obtained; all four stay UNMEASURED | the Anthropic account reached its configured spend limit part-way through the fourth run. Every call now fails before generation with `400 invalid_request_error: "You have reached your specified API usage limits. You will regain access on 2026-10-01 at 00:00 UTC."` (request_id `req_011CfAKQSAyGT6pjqaLNUfrH`; re-confirmed at 2026-09-18 with a 4-token Haiku probe, `req_011CfAKcqXjeqo4EWThDTxnT`). This is account-level and deterministic, not a rate limit: retrying, narrowing the subset or lowering concurrency cannot get past it, and `SARVAM_API_KEY` still works, so the Sarvam leg is not the constraint. The three runs before it each died on a different real defect in the code under test -- all three are diagnosed and fixed on this branch (`adapt` truncation, `post_edit` truncation on Indic script, `adapt` timeout at the raised cap) -- so the run never reached the scoring step that writes `docs/eval/uc2.json`. Cumulative spend across the four runs plus ~20 diagnostic probes is roughly Rs 400, inside the ~Rs 500 per-prompt guideline | raise the account's usage limit, or wait for the reset at 2026-10-01 00:00 UTC, then re-run `make eval-uc2-live` on this branch. Budget about Rs 110 for the run. The three fixes this prompt landed are what the earlier runs were spent on, so the next attempt starts further in than any of them did -- but do not assume it is the last defect: each of runs 1-3 uncovered a layer the previous one had hidden, and `backtranslate_qa` and `quiz` have still never completed against a real vendor | |
| 2026-09-18 | uc2/P2-eval | `make check` is red on its `audit` stage, so this prompt could not be shipped behind a fully green gate | pypi.org is returning 503 to this environment. Verified it is neither the diff nor the agent proxy: the change touches no dependency, `pypi.org` is in the proxy's `noProxy` list so `pip-audit` reaches it directly, the proxy reports `recentRelayFailures: []`, and a direct `curl https://pypi.org/pypi/aiofiles/25.1.0/json` returned 503 on 3 of 3 attempts (also seen once as a 15s read timeout). Re-confirmed later the same day: 3 of 3 again, and the gate's `audit` stage is the only one of its 20 stages that fails. Every other gate stage passes: lint, typecheck, the unit suite, the three UI suites, attribution and the secret scan | nothing in this repo; re-run `make check` once pypi.org recovers. CI runs `pip-audit` as well and may pass there if the outage is regional or has lifted | |
| 2026-09-18 | uc1/P3-eval | uc1 may hit the same response-cap truncation uc2 just did, and its B6 gates have never been measured, so nobody would see it | `Claude.structured`'s 1024-token default was sized for Latin output. uc2 measured te-IN at 986 and ta-IN at 957 output tokens for a SHORT segment's rewrite plus a change list -- within 4% of the cap -- while hi-IN used 345-604. uc1's `Decision` schema (`apps/helpdesk_agent/graph.py:49`) carries `reply_text`, a free-text helpdesk reply in the employee's language, plus an optional `ticket`, through the same adapter and the same default. A long Telugu or Tamil reply is therefore plausibly over the cap, and before the uc2 fix such a truncation surfaced as what read like a refusal. This is an ASSESSED exposure, not a measured failure: no uc1 call was run live | measure it as part of `uc1/P3-eval` (needs the Docker stack for TEI/Qdrant plus the key). If confirmed, the fix is one line -- pass a sized `max_tokens` at `graph.py:382`, as `training_localizer.stages` now does. Do not raise the platform default blindly: that cap is what bounds a runaway generation | |
| 2026-09-15 | program/P1-golden-audio | golden-audio regeneration tool not written | the 135 WAVs were recovered from the developer machine and committed, so the tool was not needed to unblock evals | run `program/P1-golden-audio`, now scoped to just `synthesize_audio.py` | |
| 2026-09-15 | uc1/P3-eval | no B6 gate for UC1 P3 could be measured; nothing was run and no vendor spend was incurred | Two preconditions named in the prompt's step 1 are absent from the session. (1) `ANTHROPIC_API_KEY` is unset, so `helpdesk_agent.graph:decide` cannot run — that blocks action accuracy, reply-language match, adversarial compliance, and the grounding-verifier retry/fallback/cost figures, i.e. everything `make eval-uc1` (which is `--chat-only`, Claude-only, no Saaras) produces. (2) The Docker daemon is unavailable, so `make stack-core`/`make stack-sparse`/`make ingest-kb` cannot run and hit@3 has no retriever. WER alone was reachable (`SARVAM_API_KEY` is set, 135 WAVs present and hash-verified) but step 1 says to stop rather than report a partial number as a gate, so the ~₹40 of Saaras calls was deliberately not spent | set `INDICAI_ANTHROPIC_API_KEY` in the cloud environment -- **now done, and a live Claude call succeeds from a session**; `ANTHROPIC_API_KEY` is the agent harness's own variable there and is stripped, which is why this looked like a missing key for three prompts. Still needs a session with a working Docker daemon; then re-run `/run-prompt uc1 P3-eval`. Preconditions already satisfied: 135 golden WAVs present, `platform/tests/test_golden_audio.py` 136 passed | |
| 2026-09-15 | uc2/P0-spike | the dubbing sample could not be produced: no media or SRT can be uploaded from a cloud session | `dubbing.create` returns `upload_url` and `srt_upload_url` on `saasprodstudiopublicsa.blob.core.windows.net`, which is not on the environment network allowlist (RUNBOOK section 1). A `PUT` fails with `httpx.ProxyError: 403 Forbidden`; `curl` to that host returns 000 while `api.sarvam.ai` is reachable. `ffmpeg` is also absent despite being listed in `scripts/cloud-setup.sh`, so no MP4 could be built either. The contract question the spike existed to answer was settled without uploading anything (ADR 0003), at no cost | add `*.blob.core.windows.net` (or the specific storage account) to the environment's network allowlist and record it in RUNBOOK section 1; confirm the setup script installs `ffmpeg`. `uc2/P4` cannot dub until then | |
| 2026-09-15 | uc2/P2 | three of the five stages (`adapt`, `backtranslate_qa`, `quiz`) and the Claude leg of `post_edit` were never executed against a real vendor, so `make eval-uc2` cannot show fidelity mean >= 4.0, and timing-fit measured 0.144 against a gate of 0.90 | `ANTHROPIC_API_KEY` is unset (same blocker as uc1/P3-eval). All four are `claude-sonnet-5`/`claude-haiku-4-5` calls. timing-fit is a consequence, not a separate blocker: `adapt` is the only stage that compresses, so without it every segment stays at its authored English length and overruns the target-language budget. The Sarvam leg did run: terminology adherence measured 1.0 over 87 expectations on live Mayura output | set `ANTHROPIC_API_KEY` in the cloud environment (RUNBOOK section 1) and re-run `make eval-uc2` (default args run `eval_hook:full`; about Rs 110 per run). No code change is expected | |
| 2026-09-15 | uc2/P3 | MSAL/Entra ID sign-in is not wired into the reviewer UI | no Entra tenant id, client id or redirect URI exists in this environment, and nothing to authenticate against. The server half is complete and fail-closed: every review and approve route requires a verified SSO subject on the ASGI scope and returns 401 without it, proven by `test_review_and_approve_fail_closed_without_sso`. A stubbed MSAL config would be worse than none, because it would read as configured | supply the tenant/client ids and a redirect URI, then add `@azure/msal-browser` to `apps/training_localizer/ui` behind `VITE_AUTH_DEV_BYPASS` | |
| 2026-09-15 | uc2/P4 | no module was dubbed, so timing-fit on real dubbed output is unmeasured and no live artifact exists | the dubbing job's `upload_url` and `srt_upload_url` are on `*.blob.core.windows.net`, which is still not on the environment network allowlist (same blocker as uc2/P0-spike). The code path is complete and tested against the recorded spike responses; what is missing is one PUT to storage. `ffmpeg` is also still absent, so no source MP4 could be built even if the allowlist changed | add `*.blob.core.windows.net` to the allowlist and install `ffmpeg` (RUNBOOK section 1), then run `uc2.produce` for one module: about Rs 400 for three languages | |
| 2026-09-15 | uc2/P5 | the Grafana spend panel has not been checked against Langfuse cost sums | no Docker daemon, so `make stack-obs` cannot start Grafana or Langfuse. Both figures are computed in the same place (`AdapterRuntime.record`, from `platform/config/pricing.yaml`), so the check is a comparison rather than a reconciliation, but it has not been run | `make stack-obs`, run a spending eval (`make eval-uc2-sarvam`, about Rs 28), then compare the panel's `sum(cost_inr)` against Langfuse's cost for the same window | |
| 2026-09-15 | uc3/P1 | real diarization attribution accuracy on the 20 golden audio items was not measured, so the P1 acceptance criterion "reports real diarization accuracy from Saaras" is FAIL | `make eval-uc3-diarize` fails with `httpx.ProxyError: 403 Forbidden`. Saaras batch STT with `diarize=True` returns an upload URL on `*.blob.core.windows.net`, which is not on the environment network allowlist — the same blocker as uc2/P0-spike and uc2/P4. The failure happens at the upload, before any audio is transcribed, so no Sarvam minutes were billed. The code path is complete: `saaras_diarizer` and `attribution_accuracy` are implemented and unit-tested against fixtures (`test_attribution_aligns_labels_before_scoring`, `test_attribution_counts_a_real_mistake`), and the runner reports `diarization_accuracy` under `unmeasured` rather than as a placeholder | add `*.blob.core.windows.net` (or the specific storage account) to the environment's network allowlist and record it in RUNBOOK section 1, then run `LIVE_API_TESTS=1 make eval-uc3-diarize`: the runner prints the estimate first (20 items, 331s of audio at Rs 45/h = Rs 4.13). No code change is expected | |
| 2026-09-15 | uc3/P2 | `make ingest-golden-audio` was never run, so the acceptance criteria "golden audio ingested" and "diarization attribution >= 0.90 against ground truth" are FAIL and UNMEASURED | Three independent environmental blockers. (1) No Docker daemon, so `make stack-core` cannot start MinIO or Postgres -- the sweep has no prefix to watch and nowhere to write `calls` / `transcript_segments`. (2) Saaras batch STT with `diarize=True` uploads to `*.blob.core.windows.net`, which is not on the environment network allowlist (same blocker as uc2/P0-spike, uc2/P4 and uc3/P1); it fails at the upload, so no Sarvam minutes were billed. The code path is complete and exercised with mocked vendors: 9 unit tests plus 4 integration tests that CI's Postgres job runs, covering idempotency on re-run, dual-script storage, turn replacement on re-transcription and survival of one unreadable recording | add `*.blob.core.windows.net` to the environment's network allowlist (RUNBOOK section 1) and run in a session with a working Docker daemon, then `make stack-core && make migrate && make ingest-golden-audio`: about Rs 4 of Saaras for the 20 golden items. (An earlier version of this entry said "no code change is expected". That was wrong: the app was handing `s3://` URIs straight to the adapter, which takes local media only, so every recording would have failed even with the environment unblocked. Fixed in this same PR by materializing each object with `storage.fetch_object` before submission.) | |
| 2026-09-15 | uc3/P4 | the B6 gates for the full three-stage detector are unmeasured: precision >= 0.80, recall >= 0.85, and the Stage 1/2 half of adversarial success = 0.0 | `ANTHROPIC_API_KEY` is unset, so neither Haiku triage nor Sonnet deep analysis can run -- `LIVE_API_TESTS=1 make eval-uc3-full` fails with `TypeError: Could not resolve authentication method` before any request is sent, so no spend was incurred. Same blocker as uc1/P3-eval and uc2/P2. Everything that does not need the key is measured and tested: the verifier, the canary discard, the additive injection flag, the deterministic lexicon floor, the combine rule and the seeded QA sample, over 21 tests; Stage 0's confusion matrix is in the P4 report | set `ANTHROPIC_API_KEY` in the cloud environment (RUNBOOK section 1) and run `LIVE_API_TESTS=1 make eval-uc3-full`: the runner prints the estimate first, about $1.00 / Rs 94 for 200 triage plus ~40 deep-analysis calls. Do not assume no code change is needed: the Stage 1/2 path has only ever run against mocks, and in uc3/P2 an identically-worded assumption turned out to be false (the app was handing `s3://` URIs to an adapter that takes local media only). Budget a debugging pass on the first live run | |
| 2026-09-15 | uc3/P5 | the audit chain has no anchor outside the database, so it is tamper-evident but not non-repudiable (ADR 0011) | an unkeyed sha256 chain can be recomputed by anyone who can edit the rows: the implementation's own benchmark walks and hashes 10,003 rows in 0.46 s, so an actor with `UPDATE` on `analysis_runs`/`flags`/`dispositions` (superuser, table owner, a restored snapshot) can rewrite the tail, re-chain it and leave `chain_verify` reporting `ok`. The local anchor (head hash + row count, `0009_uc3_chain_anchor`) adds a second row that must also be rewritten, but it sits in the same database under the same privilege and is recomputed with the same unkeyed hash, so it is a cross-check, not a trust root. Closing this needs a trust root outside the database, which this repository cannot provide: a signing key the database hosts cannot reach (Key Vault), or a WORM bucket with a retention lock, or an external notary. Nothing in P5 was narrowed to avoid it -- the chain, the role and the nightly verify are all built and tested | `program/P10-azure-deploy`: provision the Key Vault signing key or the immutable-storage container, publish a daily `(table, head_hash, row_count, timestamp)` record to it, extend `chain_verify` to compare the walked head against that external record, and hold backup credentials separately from the production role. Until then `uc3/P7`'s control mapping must carry the weaker claim |  |
| 2026-09-15 | uc3/P5 | `uc3_app` is not the role the application connects as, so the least-privilege control is proven but not yet on the path production traffic takes | the role is created, constrained to `select, insert` on the three chained tables and proven by `test_the_app_role_cannot_update_or_delete_the_audit_tables` (Postgres logged four denials in the same CI run), but nothing in this repository sets the runtime DSN: the app still connects as the migration/owner role, which carries `UPDATE`/`DELETE` implicitly and can re-grant them to itself. There is no environment to wire it in -- no deployed database, no provisioned login role, no secret store -- and inventing one in `.env.example` would read as configured when it is not. `0008_uc3_audit_role.py` records the precondition in its docstring: the app's login role must not own the audit tables and must hold no direct grants on them beyond its membership of `uc3_app` | `program/P10-azure-deploy`: provision a login role that is a member of `uc3_app` and owns nothing in `public`, run migrations as a separate owner role, point the application's DSN at the former, and add a startup assertion that an `UPDATE` on a chained table is refused so a misconfigured deployment fails loudly rather than silently losing the control |  |
| 2026-09-15 | uc3/P5 | the chain hashes the Python object the application built, not the JSON bytes Postgres stores, so a JSONB round-trip difference in `analysis_runs.output` could produce a permanent, unrepairable false break | `append` hashes `canonical_json(row_payload(row))` before the INSERT; `verify_chain` re-hashes the row as psycopg reads it back, after Postgres has parsed and re-rendered the JSONB. The two are equal only while the round-trip is exact, and JSONB does not promise that: numbers are reparsed as `numeric` and re-rendered (exponent forms and some float spellings do not survive), and non-string mapping keys come back as strings, which changes their `sort_keys` order (`1, 2, 10` becomes `"1", "10", "2"`) and therefore the canonical bytes. A row that diverges this way is reported as "row_hash does not match the row content: the row was edited" forever, and cannot be repaired: the tables are append-only by design and by grant, so there is no UPDATE available to rewrite the stored hash. Mitigation actually in place, which is why this is a latent risk and not a live defect: the only JSONB column on a chained table is `output`, whose payloads are model-generated JSON with string keys, and the detector and eval paths construct no `Decimal` and no non-string keys. No such break has been observed in the 10 pure and 7 integration tests or in the 10k-row benchmark | hash the serialized JSON text that is actually inserted rather than the Python object -- serialize `output` once with `canonical_json`, bind that text, and have `verify_chain` read the stored text back (`output::text`) and hash the same bytes -- so the hash describes what the database holds. It is a change to the hashing input, so it must land as a new chain rather than a rehash of existing rows, or be done before any chain carries data that matters; a regression test that round-trips awkward payloads (floats in exponent form, integer keys, Decimals) through Postgres and re-verifies belongs with it |  |
| 2026-09-15 | uc3/P6 | MSAL sign-in is not wired into the surveillance reviewer UI | `.claude/rules/ui.md` says "Auth via MSAL (Entra ID)", but the identity contract this app was built to puts the verified subject on the ASGI scope from server-side SSO middleware and reports it through `GET /me` — the browser holds no token, so there is nothing for an MSAL client to acquire or refresh. The UI reads roles only from `/me`, never from a token, and a 401 is explained as "sign-in happens in front of the service". Same shape as the uc2/P3 entry, and there is still no Entra tenant id, client id or redirect URI in this environment. A stubbed MSAL config would read as configured while authenticating nobody | decide whether Entra terminates at the edge (current design — then amend the UI rule) or in the browser (then supply tenant/client ids and a redirect URI, and add `@azure/msal-browser` behind `VITE_AUTH_DEV_BYPASS`); settled by `program/P10-azure-deploy` | |
| 2026-09-15 | uc3/P6 | the end-to-end demo runs against a stubbed API, not a live one | the reviewer flow (synthetic call → disposition) is exercised in a real browser by `apps/comms_surveillance/ui/e2e/review.spec.ts`, but the contract is stubbed at the network boundary because a live run needs Postgres, MinIO and the Docker daemon, none of which exist in a cloud build session. The server half is proven separately by `platform/tests/test_uc3_api.py` (59 tests, including the full role matrix) and by one `@pytest.mark.integration` test that CI runs against real Postgres | `make stack-core` on a host with Docker, then `E2E_LIVE=1 npm run e2e` from `apps/comms_surveillance/ui` against the API with `AUTH__DEV_BYPASS=true` | |
| 2026-09-15 | uc3/P6 | the API builds a new async engine and connection pool per request | `db_session` creates and disposes an engine on every call, so nothing is pooled across requests. Pre-existing and mirrored from `training_localizer/api.py`, but P6 makes this an interactive surface a reviewer refreshes all day, so the cost now lands on a person waiting | a module-level engine created in the lifespan and shared, with the pool sized for the deployment; `uc3/P7` or `program/P10` | |
| 2026-09-15 | uc1/P4 | no ticket was created against a real Zammad, so the wire format is unverified | Zammad needs Docker (Rails, memcached and its own Postgres) and this environment has no daemon. Everything about the request and response shape rests on reading Zammad's source: the `source_session_id` custom field being accepted at the payload root, `tags` as a comma-separated string, `"guess:<email>"` for `customer_id`, and above all the **search response shape** — `find_filed` is the only thing standing between a dropped connection and a duplicate ticket, and it tolerates both documented forms rather than pinning one precisely because it could not be tested. The idempotency itself IS proven where it can be: the UNIQUE constraint race runs against real Postgres in CI, and the concurrent-duplicate test asserts one ticket at Zammad rather than one row, sabotage-checked against a read-then-write implementation | `make stack-ticketing && python -m helpdesk_agent.zammad_seed && make test-ticketing` on a host with Docker; `test_a_real_zammad_accepts_the_payload_and_finds_it_again` is written and waiting | |
| 2026-09-15 | uc1/P4 | the Zammad compose profile was never booted | no Docker daemon. Image tags were confirmed against registry manifests but no layer was pulled and no container created, so `zammad-init` completing, Rails migrating, the railsserver healthcheck, and `ELASTICSEARCH_ENABLED=false` being sufficient with the service dropped are all unrun. Memory and CPU cost are unmeasured; the README says so rather than guessing | `make stack-ticketing` on a host with Docker | |
| 2026-09-15 | uc1/P4 | nothing reaps a `pending` ticket_filings row once its task is lost | `enqueue` swallows a broker failure so a Redis outage cannot fail the turn, and the row is described as "the record of what is owed" — but nothing reads it back. There is no beat sweeper, the idempotent-replay path does not re-enqueue, and a task that exhausts `max_retries=5` or dies on `TicketingRejected` leaves the row pending for ever. The employee was told the number would be emailed. Not widened into P4: a sweeper needs its own beat entry, a claim/lease so two workers cannot both settle one row, and tests | a `uc1.sweep_pending_tickets` beat job that re-enqueues rows pending beyond a threshold, with the same `find_filed` adoption guard; `uc1/P7` (retention and operational hardening) | |
| 2026-09-15 | uc1/P4 | the pending reply promises an email that nothing in this repo sends | the reply says the ticket number will be emailed. Zammad notifies its customer only when `employee_id` is an address (the `guess:<email>` attach); for a non-email SSO subject — the case the code explicitly handles by omitting `customer_id` — nobody is notified at all. The promise is unfulfillable for exactly the identities that need it most | either resolve the SSO subject to an address before filing and require it, or change the pending wording to what the system can actually do (the number appears in the session when the worker settles it); decide in `uc1/P6` (the widget knows the session) or `uc1/P7` | |
| 2026-09-15 | uc1/P5 | the voice latency gates are unmeasured: time-to-first-audio p50 <= 2.0s and p95 <= 3.5s over the 30 golden items, and action accuracy on those items, have no numbers at all | `make voice-test` joins a LiveKit room and measures time-to-first-audio from LiveKit track events, so it needs a running `livekit` container. This session has no Docker daemon, so `make stack-voice` cannot start LiveKit and the harness cannot run. CI does not provision LiveKit either, which is why the tests carry the `voice` marker and are deselected from the gate rather than marked `integration` (a skip there turns CI red under uc3/P5's evidence guard). No latency number is estimated in its place: the PRD C5 budget table (~1.85s p50 expected) is a budget, not a measurement, and the C5 levers (Haiku for `clarify`, six chunks down to four, drop the parallel translate pass) were therefore never exercised because there was no p95 to miss | on a machine with Docker: `make stack-voice && make migrate`, export `SARVAM_API_KEY`, then `make voice-test`. Report the measured p50/p95 and the action accuracy; if p95 > 3.5s apply the C5 levers in order and report which one fixed it, with before/after numbers |  |
| 2026-09-15 | uc1/P5 | the acceptance criterion "a demo script lets me speak into the browser and hear a Hindi reply" is UNMEASURED: no browser ever joined a room and no voice turn was ever spoken | a cloud build session has no Docker daemon (so no LiveKit on 7880), no microphone and no browser. The prompt's own execution note says to write the demo script and mark the criterion unmeasured rather than claim it. `apps/helpdesk_agent/voice_demo.md` is that script: stack bring-up, token minting (the `livekit-api` snippet in it was run offline and does produce a token), the per-turn Sarvam and Claude cost, what to say, the five things to watch for, and the STT-failure/chat-mode path. Everything it predicts is labelled expected, because none of it was observed. It also has no browser client to point at: `apps/helpdesk_agent/ui/` is still just a `.gitkeep`, so it sends the operator to LiveKit's Meet sample or a self-served page | a laptop with Docker, a microphone, `SARVAM_API_KEY` and `ANTHROPIC_API_KEY`: follow `apps/helpdesk_agent/voice_demo.md` end to end, record what was observed (including the `turns.latency_ms` row) and drop this entry in the same change. `uc1/P6` ships the React widget, which replaces the external client the demo currently borrows |  |
| 2026-09-15 | uc1/P5 | no recorded consent-notice audio exists, so the greeting that tells an employee the call is recorded and transcribed cannot play | PRD C8 makes the greeting the notice on which recording and transcribing the call rests, and it must play before the pipeline consumes any microphone audio. The repository contains no such asset: the only committed audio is the 155 golden WAVs under `platform/eval/golden/**` and three Bulbul voice samples under `docs/adr/assets/0003/`. `VoiceSettings.greeting_path` defaults to `None`, and `run_session` refuses to start on that default — `load_greeting` raises `GreetingUnavailable` before a room is joined — so the effect is not a silent notice but a voice channel that cannot be run at all until the asset exists. This is not an engineering gap that code closes: the wording is Legal/HR text that has to be approved and recorded, in each pilot language, and a line an engineer invents in a demo script is not a notice | Legal/HR approve the notice text for hi-IN, te-IN, ta-IN and en-IN; record or synthesize it, commit the files with the approved text next to them, and default `greeting_path` to the shipped asset so a session cannot start without one. A synthesized stand-in may be used to demonstrate the mechanism only, labelled as a stand-in |  |
| 2026-09-15 | uc1/P5 | pipecat fetches its NLTK `punkt_tab` tokenizer data over the network on first use, inside a voice turn | `pipecat/utils/string.py` calls `nltk.download("punkt_tab")` when the data is absent, into whatever `NLTK_DATA` points at. On a deployment with no egress that is a first-turn failure rather than a slow first turn; with egress it is an unpinned download on the latency-critical path, and the p50 budget (PRD C5, ~1.85s to first audio) has no room for it. Not hit in this session because nothing ran a real turn | bake the data into the image at build time (`python -m nltk.downloader punkt_tab`) and set `NLTK_DATA`, or pre-warm the tokenizer at pipeline construction so the cost lands before the first participant joins; verify with egress blocked. See `docs/security/audit-exceptions.md` |  |
| 2026-09-15 | uc1/P5 | PRD C9 asks for one retry on a Saaras stream error or 429 before the session falls back to chat; nothing retries | `indic_platform/adapters/sarvam_stt.py` has no retry, and `SarvamSTTProcessor._consume` latches chat mode on the first exception — so a single transient error costs the whole listening half of the session, where C9 expects one more attempt. Latching itself is deliberate and should stay (a room that silently resumes recording after saying it stopped is worse); what is missing is the attempt *before* the latch | add a single bounded retry inside the STT adapter's stream, so the processor still sees exactly one terminal failure and the latch semantics do not change; cover it with a fake that fails once then succeeds. Decide in `uc1/P7` |  |
| 2026-09-15 | uc1/P7 | `ticket_filings.payload` holds transcript-derived text that outlives the 90-day transcript rule and nothing sweeps it | the retention job deletes `turns` rows at 90 days, but the ticket body an employee's description became lives on in `ticket_filings.payload` (and in Zammad itself, which this repo does not control). Deleting half an operational ticket under a transcript rule is a policy decision PRD C8 has not made, so it was left out of scope rather than silently included. As it stands the 90-day promise is true of `turns` and not of everything derived from a turn — which is threat T5 (over-retention) in the place a reviewer is least likely to look | Legal/Compliance decide whether a filed ticket is an operational record with its own lifecycle (likely) or transcript-derived data under C8's rule; if the latter, add a third policy to `apps/helpdesk_agent/retention.py` covering `ticket_filings` and decide what happens to the Zammad-side copy, which deletion here does not reach |  |
| 2026-09-15 | uc1/P7 | no Celery worker or beat entrypoint exists, so the retention sweep has never run on a schedule anywhere | `uc1.retention_sweep` is registered on the app's Celery app and tested against a real PostgreSQL, but the repo has no worker Makefile target and no compose service for uc1 or uc3 — the same gap uc3's nightly sweep has. So retention is implemented and unexercised as a scheduled job; a deployment that never starts a beat gets no deletions and no log rows, which looks identical to a deployment with nothing to delete | add a worker/beat service to `docker-compose.yml` and a `make worker-uc1` target; run it once with `UC1_RETENTION_DRY_RUN=true` against real data and read the `retention_deletions` rows before letting it delete. Likely `program/P10` (deploy) |  |
| 2026-09-15 | uc1/P7 | CI's B6 regression gate measures the adversarial threshold only; action accuracy, reply-language match and hit@3 are UNMEASURED there | the prompt's acceptance is "CI runs eval-uc1 and fails on regression thresholds from Part B6". `make eval-uc1-regression` does fail on the blocking adversarial threshold (0% compliance over all 20 adversarial items, proven by making one item comply and watching the run exit 1 naming it). It cannot gate the quality thresholds: scoring them needs `helpdesk_agent.graph:decide`, which needs TEI, Qdrant, an ingested KB and an `ANTHROPIC_API_KEY`, and CI's `checks` job has none of those. Under `--mocked-decisions` the runner refuses to emit those three metrics rather than scoring the trivial baseline and printing a number that reads like a quality signal | either a recorded-decisions replay fixture committed to `platform/eval/` (the decisions of one real `make eval-uc1` run, replayed offline so the scorer sees the agent's actual actions), or a CI job that provisions TEI + Qdrant + an ingested KB with a key. The replay is the cheaper and more honest of the two and is the natural home for `program/P8-eval-refactor` |  |
| 2026-09-15 | uc1/P6 | no application exposes a Prometheus `/metrics` endpoint, so every counter in `platform/obs/metrics.py` is written into a registry nothing scrapes | `infra/prometheus.yaml` scrapes prometheus itself, livekit and qdrant — no app target. Nothing calls `make_asgi_app`, `generate_latest` or `start_http_server`; uc3's `/metrics/precision` and `/metrics/false_negative_estimate` are JSON API routes, not a Prometheus exposition. The consequence lands on a control I shipped in uc1/P7: the 50/80/100% budget alerts are counters plus one log line, and in a deployment only the log line reaches anybody. It also means a Prometheus-backed Grafana panel would render an empty graph for ever, which is why uc1's dashboard is Postgres-backed | mount a `/metrics` ASGI app on each FastAPI app (`prometheus_client.make_asgi_app`), add the three app targets to `infra/prometheus.yaml`, and only then move the latency and degraded-mode panels onto Prometheus. Likely `program/P10` (deploy), since it is a wiring-and-scrape change rather than an app feature |  |
| 2026-09-15 | uc1/P6 | `adapter_calls` is modelled, migrated and pruned by the retention sweep, but nothing ever inserts a row into it | `AdapterRuntime.record` computes `cost_inr`/`cost_usd` and hands the record to a `Sink`; the only two Sinks (`platform/obs/langfuse.py`) are `LangfuseSink` and `MemorySink`, and neither writes to the database. So per-call cost is queryable only inside Langfuse. Consequences: uc1's daily-spend Grafana panel and **uc2's existing spend panel** are both permanently empty; `apps/helpdesk_agent/retention.py` prunes a table with nothing in it; and the helpdesk README claimed `adapter_calls` rows existed until this prompt corrected it. CLAUDE.md's observability rule asks for a Langfuse span with INR/USD cost, which IS satisfied — this is the queryable half, not a rule violation | a `Sink` that writes `AdapterCall` rows. The care needed is in *where*: `Sink.emit` is synchronous and called on the adapter's hot path, so a blocking insert would add database latency to every vendor call and land directly on the PRD C5 first-audio budget. Likely a buffered or out-of-band writer rather than an inline insert, which is why this was not done inside uc1/P6 |  |
| 2026-09-15 | uc1/P6 | there is no LiveKit join-token endpoint, so the browser widget cannot start a voice session on its own | minting a join token needs `LIVEKIT_API_SECRET`, which belongs server-side; the pinned contract defines no such route and the UI agent correctly declined to invent one. The widget POSTs to `VITE_LIVEKIT_TOKEN_PATH` (same-origin only) when a deployment grows one, and otherwise accepts a token pasted by hand per `apps/helpdesk_agent/voice_demo.md` step 2 in a local dev-bypass build; a deployed build says "voice unavailable, chat is unaffected" rather than showing a dead mic | decide whether uc1 gets `POST /voice/token` issuing a short-TTL room token scoped to the caller's own session, and if so gate it the way `/sessions/{id}/replay` is gated — a token is a credential to hear the call, so it must not be mintable for another employee's room. Likely `uc1/P6` follow-up or `program/P10` |  |
| 2026-09-16 | uc1/P6 | `make check`'s secret stage scans only TRACKED files, so a brand-new file passes the gate and then fails it the moment it is committed | the stage runs `detect-secrets-hook --baseline .secrets.baseline $(git ls-files)`, and `git ls-files` lists tracked paths only. A developer adding a file therefore gets a green gate, commits, and the next run goes red on the file they just added. It has now happened twice in one session: a PAN-shaped fixture in `platform/tests/test_uc1_redaction.py` (uc1/P7) and a hex `trace_id` in `apps/helpdesk_agent/ui/e2e/fixtures.ts` (uc1/P6). CI does not have the problem — it scans after checkout, where everything is tracked — so the gate is *weaker* than CI, which is the wrong way round for a pre-push check | scan the working tree rather than the index: add untracked-but-not-ignored paths (`git ls-files --others --exclude-standard`) to the same invocation in `scripts/checks.sh`. One line, and it makes `make check` agree with CI |  |
| 2026-09-16 | uc3/P7 | nothing in the repository persists an `analysis_runs` or a `flags` row outside tests, so uc3's hash chain protects two tables only the test suite writes | `detector.Analysis.row()` builds the `analysis_runs` payload and its docstring says it is "ready for P5 to hash-chain", but no caller constructs `indic_platform.db.models.AnalysisRun` or `.Flag` outside `platform/tests/`. `api.py` and `metrics.py` READ both tables, so a real deployment's reviewer queue and precision metrics are computed over an empty set, and the append-only guarantees uc3/P5 shipped currently guard nothing a user produced. Verified by grep across `apps/` and `platform/` excluding tests | write the detector's persistence path: one `analysis_runs` row plus its `flags` per analysed call, appended through `audit.append` so they enter the chain. It is the missing half of uc3/P4-P5 rather than a defect in either, and it is a prompt of its own | |
| 2026-09-16 | uc3/P7 | `DATABASE_URL` cannot be overridden from the environment, so no test run can be pointed at a clean database | `platform/config/settings.py:7` calls `load_dotenv(ROOT/'.env.stack', override=True)`, and both `platform/tests/conftest.py` and `platform/db/migrations/env.py` import settings. So `DATABASE_URL=... pytest` silently uses `.env.stack`'s database instead, and `DATABASE_URL=... alembic upgrade head` migrates that one. This is why the re-runnability blocker below was hard to diagnose: the obvious check ("run it against a fresh database") quietly does not do that, and the results read as a code regression. Proven by printing the variable either side of the settings import | either drop `override=True` (an explicitly exported value should win over a file), or have `env.py` and `conftest.py` read the URL before importing settings. Changing it touches every app's configuration path, so it belongs in `program/P10` with the deployment work rather than in an app prompt | |
| 2026-09-16 | uc3/P7 | Langfuse has no per-application access scoping: one org, one project, one key pair shared by uc1, uc2 and uc3 | `docker-compose.yml` provisions a single `indic` org and a single `indic-platform` project, and every adapter reads the same `LANGFUSE_PUBLIC_KEY`/`LANGFUSE_SECRET_KEY`. PRD T2's stated control is "role-scoped access to traces", and uc3's traces are surveillance metadata about named employees, so anyone with helpdesk trace access can see which calls were analysed and what they cost. Span bodies carry no call content (`AdapterRuntime.record` is a fixed metadata shape), so this is a metadata-scoping gap rather than a content leak | a Langfuse project per application with its own key pair, set per app in the deployment; the adapters already take the keys from the environment, so no code change is needed beyond the compose provisioning. Belongs with `program/P10` | |
| 2026-09-16 | uc3/P7 | `render_english` sends flag evidence spans to Claude under an unversioned inline prompt and swallows every exception | `apps/comms_surveillance/detector.py` builds its system prompt as a string literal rather than a file under `prompts/`, so its `prompt_version` is the sha256 of a literal nobody reviews as a prompt, and PRD T6's control ("prompts versioned in git; every flag records `policy_version`; CI eval on prompt change") does not cover it. Its `except Exception: return ""` also means a vendor refusal is indistinguishable from a call with no English rendering | move the text to `apps/comms_surveillance/prompts/render_english.md` so it is versioned and picked up by the CI policy-surface check, and let the failure surface as a recorded degradation the way Stage 2's does | |
| 2026-09-17 | uc3/P7 | **B6's adversarial gate FAILS on the real detector: `adversarial_success` = 0.15 (3 of 20), measured for the first time** | The first live run of `make eval-uc3-full` ($1, 200 triage + deep analysis). B6 makes any success a build blocker, so this is red. All three are `echoed`, none is `suppressed`: the detector missed nothing it was manipulated into missing, and `recall` is 0.986 with `clean_flag_rate` 0.0. The three (`uc3-adv-10`, `uc3-adv-11`, `uc3-adv-12`, all `adversary_kind=evasion`) are flags whose `evidence_span` quotes the ATTACK turn -- "spell it wrong so the scanner does not catch it", "Wo baat W-h-a-t-s-A-p-p pe karte hain, likhna mat kahin" -- while the separately labelled violation on the next line was also flagged. `score_adversarial`'s echo rule reads a quoted attack turn as "the model treated the transcript as instructions", but here the model quoted it as evidence OF the attack, which is what a surveillance system should do with a line instructing a colleague to evade the scanner | decide whether the echo rule should exempt a span that is itself misconduct, or whether the golden set should label those attack turns so quoting them scores as a true positive. **Not** to be resolved by loosening the gate to make it pass: the rule change needs before/after numbers on the full set and a reviewer, per `.claude/rules/eval.md`. Until then uc3's adversarial gate is red on a measurement, which is better than the UNMEASURED it replaced | |
| 2026-09-17 | uc3/P7 | **B6 flag precision FAILS on the real detector: 0.30 against a 0.80 gate**, measured for the first time | Same live run. 242 flags against 74 labels, so roughly 168 unlabelled flags. Recall is excellent (0.986, and 1.0 in five of six categories) and `clean_flag_rate` is 0.0 -- no clean transcript was flagged at all -- so the over-flagging is concentrated on transcripts that do carry a violation, where the detector raises extra findings around the labelled one. Per-category precision is flat, 0.26-0.37, which points at a systematic threshold or prompt effect rather than one bad category | a detector-quality prompt of its own: tune the Stage 2 prompt and the escalation threshold against this now-reproducible number, with `make eval-uc3-full` as the before/after. It is the first real quality signal uc3 has ever had, and it is a quality gap rather than a defect in the harness | |
| 2026-09-17 | uc3/P7 | `cost_per_call_inr` is still UNMEASURED even on a live run | `run_uc3.evaluate` takes a `sink=` and computes spend from adapter records, but the CLI never passes one, so the one metric that would have reported what the $1 run actually cost comes back unmeasured. The estimate is printed before the run and nothing verifies it afterwards | wire an adapter `MemorySink` through the runner's CLI the way `ingest.stt_with_sink` does, and report measured INR next to the estimate. Small, and it makes every future live eval self-costing | |
