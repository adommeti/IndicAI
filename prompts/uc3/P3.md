# P3 — Lexicon matcher

<!-- Run: bash scripts/run-prompt.sh uc3 P3 -->
Read CLAUDE.md. Create apps/comms_surveillance/lexicon/{category}.yaml for the six categories with ≥ 25 entries each: native-script variants (hi/te/ta), Roman transliteration variants, optional regex, weight, severity. Mark the files DRAFT — Compliance owns them. Implement lexicon/matcher.py: tokenization-aware matching over native + transliterated segment text, returning hits with spans, category, severity, and lexicon_version (git SHA). Wire into the runner as Stage 0 and report recall by category for lexicon-only.
Acceptance: lexicon-only recall reported per category; unit tests for script variants and regex; matcher is pure (no I/O) and fast (< 50ms per call transcript). Include in the report: recall-by-category.

## Execution notes
- Pure Python matcher with no I/O; benchmark test asserts < 50 ms per transcript on the golden set. Lexicon YAMLs are DRAFT and Compliance-owned: say so in each file header.
- `lexicon_version` = short git SHA of the lexicon directory tree (`git rev-parse HEAD:apps/comms_surveillance/lexicon`), computed at import with a fallback hash of file contents when not in a git checkout.
