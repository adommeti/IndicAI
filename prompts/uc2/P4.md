# P4 — Production: dubbing, TTS, captions, packaging

<!-- Run: bash scripts/run-prompt.sh uc2 P4 -->
Read CLAUDE.md and docs/adr/0003. Implement Celery tasks dub, tts_summary, captions, package. dub follows the ADR's chosen path; poll with capped backoff; store dubbed MP4 in MinIO. captions builds WebVTT per language from approved segments and timings. tts_summary renders a Claude-written 5-minute summary script (add a summary prompt) via Bulbul with the language's default voice. package writes a manifest.json with URIs, sha256, versions (glossary, prompts, models), and marks the module-language as delivered. Measure actual timing-fit on the dubbed output (compare per-segment durations from the API's timing data to source) and store it.
Acceptance: for one module in all three languages, artifacts exist with checksums; VTT validates; timing-fit ≥ 90%; a re-run after a single segment edit only re-produces, it does not re-translate. Include in the report: the manifest.

## Execution notes
- Follows `docs/adr/0003`. Dubbing for one module in three languages costs ≈ ₹40 × minutes × 3; the golden sample modules are short (~3 min each): budget ≈ ₹400 and print it first. If the key or credit is unavailable, run everything with the recorded spike responses as fixtures and mark the live criteria UNMEASURED.
- VTT validation: use a strict parser (`webvtt-py`) in a test. Timing-fit is computed from the API's per-segment timings and stored in `artifacts.meta`.
- Re-run semantics test: edit one approved segment → only `dub|tts|captions|package` tasks execute (assert translate/post_edit task functions are not called).
