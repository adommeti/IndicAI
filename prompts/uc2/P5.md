# P5 — Pilot delivery page and comprehension report

<!-- Run: bash scripts/run-prompt.sh uc2 P5 -->
Read CLAUDE.md. Add a minimal delivery route in the UI: employee picks language, watches the dubbed video with VTT captions, takes the quiz; store attempts. Add GET /reports/comprehension returning pass rate, mean score, and time-on-task by language and by cohort (native-language vs english-only control, cohort assigned by config), plus a Markdown summary. Add a Grafana panel for localization throughput and per-module INR/USD spend.
Acceptance: end-to-end demo from upload to a quiz attempt recorded; report renders; spend panel matches Langfuse cost sums. Include in the report: a pilot-run checklist.

## Execution notes
- Delivery route in the same UI package; quiz attempts table via a new alembic revision.
- The comprehension report is computed by SQL in a repository module with a unit test on a seeded SQLite/Postgres fixture; cohort assignment from config.
- Grafana panel JSON under `infra/grafana/dashboards/uc2.json`; the "spend panel matches Langfuse sums" criterion needs `make stack-obs` — UNMEASURED otherwise, with the query used.
