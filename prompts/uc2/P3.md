# P3 — Reviewer UI

<!-- Run: bash scripts/run-prompt.sh uc2 P3 -->
Read CLAUDE.md. Build apps/training_localizer/ui (React + Vite + Tailwind): a module list; a segment review table with columns source | translation | back-translation | QA score | glossary hits | change log; LOCKED rows visibly badged and diff-checked against approved_renderings (red if mismatch); inline edit with save creating a new 'approved' version row with reviewer id; a "flagged only" filter (score ≤ 2 or glossary miss); approve-all-remaining with confirmation; quiz item review with approve toggle. Auth via Entra ID (dev bypass flag). Add GET /modules/{id}/review payload and PUT /modules/{id}/segments/{seg}/approve.
Acceptance: a reviewer can approve a full module in under 10 minutes for 20 segments (time it with a script); every edit is recorded with who/when; LOCKED mismatch cannot be approved without an explicit override reason. Include in the report: screenshots.

## Execution notes
- UI in `apps/training_localizer/ui`; follow `.claude/rules/ui.md`. The "under 10 minutes for 20 segments" criterion is measured with a Playwright script that drives the review flow against the API with the dev-bypass identity (skipped unless `E2E=1`; UNMEASURED in this session if it cannot run). Screenshots: capture with Playwright into `docs/build/screenshots/uc2/` (≤ 500 KB each).
- LOCKED override requires a reason string ≥ 10 chars, stored with reviewer id; enforced by the API, tested.
